# Screenshots

Put PNG screenshots here for the README + Realisation chapter. Recommended captures:

- `dashboard.png` — full dashboard at 1440 px width.
- `dashboard-mobile.png` — same page at 375 px width.
- `documents.png` — list page with filters and a few rows.
- `documents-mobile.png` — card layout at 375 px width.
- `document-detail.png` — split-pane PDF viewer + HITL form with the "Detected as INVOICE (94%)" badge visible.
- `document-detail-mobile.png` — stacked layout at 375 px width.
- `upload.png` — upload page with the new "AI will detect the document type" hint and at least one file in the queue.
- `login.png` — login screen with the branding panel.
- `admin.png` — admin overview page.

Use a consistent browser chrome (or none) and keep the dark/light theme consistent across the set. PNG @1× or @2× is fine for the report.
